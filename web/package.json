{
  "license": "MIT",
  "scripts": {
    "build": "lerna run build",
    "prod": "lerna run prod",
    "both": "run-s build prod",
    "test": "run-s build",
    "clean": "lerna clean --yes && node rimraf-standalone.js node_modules \"**/yarn-*.log\" \"**/lerna-*.log\" \"**/npm-*.log\"",
    "postinstall": "lerna bootstrap",
    "run:admin": "lerna run start --stream --scope @devbhuwan-salesmanager/admin",
    "run:agent": "lerna run start --stream --scope @devbhuwan-salesmanager/agent",
    "run:portal": "lerna run start --stream --scope @devbhuwan-salesmanager/portal",
    "run:server": "lerna run start --stream --scope @devbhuwan-salesmanager/sale-rest-server",
    "admin": "run-p -l run:admin run:server",
    "agent": "run-p -l run:agent run:server",
    "portal": "run-p -l run:portal run:server"
  },
  "devDependencies": {
    "@angular/common": "^5.0.0",
    "@angular/compiler": "^5.0.0",
    "@angular/core": "^5.0.0",
    "@angular/forms": "^5.0.0",
    "@angular/http": "^5.0.0",
    "@angular/platform-browser": "^5.0.0",
    "@angular/platform-browser-dynamic": "^5.0.0",
    "@angular/router": "^5.0.0",
    "core-js": "^2.4.1",
    "lerna": "^2.8.0",
    "npm-run-all": "^4.1.2",
    "rxjs": "^5.5.2",
    "zone.js": "^0.8.14",
    "codelyzer": "^4.0.1",
    "tslint": "~5.7.0",
    "typescript": "~2.4.2"
  }
}
