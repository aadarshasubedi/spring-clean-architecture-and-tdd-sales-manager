import {TestBed} from '@angular/core/testing';

describe('DestroyLocalSessionUseCase Unit Tests', () => {


  beforeEach(() => {
    TestBed.configureTestingModule({
      imports: []
    })
      .compileComponents().then(() => {
    });
  });

  it('should created', () => {

  });
});
